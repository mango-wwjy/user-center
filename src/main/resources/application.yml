server:
  port: 9001
spring:
  profiles:
    active: dev

mybatis-plus:
  mapperPackage: com.mango.usercenter.system.**.mapper
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  typeAliasesPackage: com.mango.usercenter.system.**.domain
  checkConfigLocation: false
  configuration:
    # ?????????camel case???
    mapUnderscoreToCamelCase: true
    # MyBatis ??????
    # NONE???? PARTIAL?????? resultMap ???? FULL???? resultMap ????
    autoMappingBehavior: PARTIAL
    # MyBatis ????????????????
    # NONE????? WARNING??????? FAILING??????????
    autoMappingUnknownColumnBehavior: NONE
    # ???????? ?????? org.apache.ibatis.logging.stdout.StdOutImpl
    # ?????? (????? p6spy ??) org.apache.ibatis.logging.nologging.NoLoggingImpl
    # ?????? org.apache.ibatis.logging.slf4j.Slf4jImpl
    logImpl: org.apache.ibatis.logging.nologging.NoLoggingImpl
  global-config:
    # ???? Logo banner
    banner: true
    dbConfig:
      # ????
      # AUTO ?? NONE ? INPUT ???? ASSIGN_ID ?? ASSIGN_UUID ?? UUID
      idType: ASSIGN_ID
      # ??????
      logicDeleteValue: 2
      # ??????
      logicNotDeleteValue: 0
      # ??????? insert,? insert ??????????
      # IGNORED ?? NOT_NULL ?NULL NOT_EMPTY ?? DEFAULT ?? NEVER ??? SQL
      insertStrategy: NOT_NULL
      # ??????? update,? update ??????????
      updateStrategy: NOT_NULL
      # ??????? select,? select ??????????? wrapper ???? entity ??? where ??
      where-strategy: NOT_NULL

